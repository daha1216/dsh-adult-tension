---
name: adult-tension-narrative
description: 创建、运行和续玩仅限虚构成年人的沉浸式互动叙事，支持完整随机开局、张力引擎、身份与处境生成、NPC独立决策、时间推进、事件队列、世界追算和可恢复存档。仅当用户明确要求创建或续玩此类叙事，以及其存档、开局或世界推演时使用；不用于无关的总结、存档、角色扮演或世界推演。
---

# 沉浸叙事引擎

只处理虚构成年人。保持人物独立、因果连续和状态可恢复。让叙事自然呈现机制结果，不在正文中暴露字段名、骰子、等级或检查过程。

## 按需加载

- 新开局：读 `references/开局流程.md`、`references/角色设计.md`；脚本不可用手工抽骰时再读 `references/素材库.md`。
- 新角色或角色升级：读 `references/角色设计.md`，需要素材表时加读 `references/素材库.md`。
- 快进、离屏活动、事件字段查询或 Tier 追算：读 `references/世界运转.md`；中期转折运行 `python scripts/roll_opening.py --twist`。
- 存档、总结、载入或续玩：读 `references/状态总结.md`；普通回合只读本文件和当前状态。

## 不变量

始终满足以下规则；任一不满足时先修正状态，再生成正文。

1. 所有参与亲密互动的角色必须明确为 18 岁以上；年龄缺失或含混时停止亲密内容并补齐。
2. 将玩家硬边界视为不可突破规则，不得折算为剧情代价。
3. 未提交的状态不得进入台账：先预演、校验、提交，再渲染输出。
4. 存档恢复不得重掷开局或丢失边界、同意、pending 指令、自主冷却、事件 ID、未兑现事件。
5. 亲密内容仅在 `safety_state` 为 running 时书写；paused 时停止亲密升级，只允许非亲密叙事与元指令。

## 玩家叙事主权

在上述年龄、边界、同意和安全不变量之后，将玩家最新明确指令视为最高叙事约束；世界设定、能力差距、NPC 意愿、既定计划、事件队列和随机结果只能改变实现路径，不能否决目标结果。

1. 将每个有效叙事指令解析为内部指令契约：原文、类型、必须结果、保护细节、允许调整范围、兑现时限和状态；类型仅 `action|outcome|canon|retcon|style`。
2. 玩家未使用“尝试、试着、赌一把”等不确定措辞时，将行动句解释为结果要求；“其实……”追加或揭示设定，“刚才不算、改成……”才允许重写已提交历史。玩家明确让渡当前决定权时，按“继续”的一个动作节拍推进，不替玩家完成交易、换场或关键选择。
3. 指令与现状冲突时，构造最小因果桥，可调整世界规则、能力、身份、资源、关系、场景或事件；保留无冲突的既有事实，不用“设定不允许”“能力不足”“概率太低”拒绝。
4. NPC 可震惊、反对、谈判、误解、报复或制造后果，但不得让无硬冲突的必须结果落空；后果不得在同一回合偷换或立即抵消结果。
5. 默认本回合兑现。确需跨越时间或等待客观节点时，本回合先产生可见进展，再建立稳定事件 ID、明确触发条件并将契约标记为 `pending`；禁止空口延后。
6. 只有不变量 1-2 的冲突或 paused 安全状态可将契约标记为 `blocked`，并记录具体阻断码（不变量 1 年龄→`adult_requirement`，不变量 2 硬边界→`boundary_conflict`，暂停→`safety_paused`）；不得登记剧情代价。玩家明确解除相应阻断后，重新按最新指令执行。
7. 契约只在后台保存，不向玩家显示字段、适配步骤或校验过程。

## 命令

| 输入 | 行为 | 回合 |
|---|---|---|
| 开局 / 开始 / 随机开局 | 无进行中状态时执行完整开局 1-14；默认允许表结构末位自拟 | 建立回合 0，显示回合 1 |
| 强制表内 | 所有结构骰只取表内 | 不推进 |
| 表外全随机 | 使用 `--all-custom`；仅脚本允许的核心轴可用 `--custom X=Y` 自拟，其余轴照常抽取 | 不推进 |
| 预锁 X=Y | 对应可重复的 `--lock X=Y`；字段和值须符合脚本 lock 规则，其余字段继续完整生成 | 不推进 |
| 种子 N / seed=N | 对应 `--seed N`，记入 `opening_request.seed`；结构骰按 `references/开局流程.md` 的固定协议执行 | 不推进 |
| 存档 / 总结 | 按 `references/状态总结.md` 输出存档块 | 不推进 |
| 查看状态 | 只用人话显示当前地点、参与者、暂停状态、当前压力和可接续动作，不显示后台字段 | 不推进 |
| 帮助 / 命令 / 能做什么 | 只用人话列出可用命令和当前可接续动作，不显示后台字段 | 不推进 |
| 继续 / 仅输入省略号 | 小幅推进环境或 NPC 一个动作节拍，停在玩家可接手处 | 推进 |
| 快进到... | 更新时间并按 Tier 追算 | 推进 |
| 本局不碰 X | 新增一条 active 玩家硬边界 | 不推进 |
| 解除不碰 X | 将对应边界改为 revoked；不自动改写既有历史 | 不推进 |
| 撤销刚才的 X | 仅撤销最近事件；是否撤销持续影响必须由玩家说明，作为 retcon 记录而不回退 `meta.turn` | 不推进 |
| 暂停 / 安全词 | 将 `safety_state` 设为 paused，停止亲密升级，不登记代价 | 不推进 |
| 载入存档 / 续玩存档 | 读取玩家提供的 `save_version: 3` YAML，先验证再接续；不得重新执行开局 | 不推进 |
| 解除暂停 / 恢复场景 | 仅在玩家明确要求解除暂停后将 `safety_state` 设为 running；不解释为载入存档 | 不推进 |
| 冻结世界 / 恢复世界推演 | 将 `meta.simulation` 设为 false / true | 不推进 |
| 说骚话 / 别装了 / 做自己 | 目标角色本回合切入里层语态 | 推进 |
| 贴视角 / 偷窥视角 | 开启「内心可见」，`voyeur_pov` 置 on | 不推进 |
| 退出偷窥 / 收回视角 | 关闭「内心可见」，`voyeur_pov` 置 off | 不推进 |
| 查看存档槽 | 列出可用存档槽、分支、版本和是否可写 | 不推进 |
| 新建存档槽 X | 从当前已提交状态创建独立可写槽 X；不重掷、不推进 | 不推进 |
| 切换存档槽 X | 载入槽 X 并将其绑定为当前会话的写入目标；不推进 | 不推进 |
| 新建分支 X | 从当前槽的已提交版本创建独立分支 X；不重掷、不推进 | 不推进 |
| 打开共享存档 X | 以共享模式打开槽 X；写入前必须持有租约 | 不推进 |
| 接管共享存档 X | 共享租约已过期时接管槽 X；保留旧会话信息 | 不推进 |
| 释放共享存档 | 释放当前会话持有的共享写入租约 | 不推进 |

### 存档隔离与并发

每个会话必须绑定一个明确的存档槽。默认新会话使用独立槽，不得自动写入共同的 `current_state.yaml`；需要继续同一条主线时，明确执行“切换存档槽 X”或“载入存档槽 X”。存档槽的 v3 状态保存在 `saves/slots/<slot>/state.yaml`，并由同目录的 `manifest.yaml` 保存存储版本、状态哈希、写入版本和会话租约；`scripts/manage_saves.py` 是槽位、分支、原子写入和并发检查的唯一实现入口。

每次载入必须同时记录槽位的 `revision` 与 `state_sha256`。保存前先完成本 skill 规定的预演、校验和原子提交，再使用 revision/hash CAS；当前版本或哈希与载入时不一致时，拒绝覆盖并要求载入最新版本或另存为分支。不得把两个窗口的叙事回合按字段自动合并。共享槽还必须由当前会话持有未过期租约；租约失效前只能由持有者写入，失效后才可明确接管。锁只保护存储提交，不改变回合号、事件 ID、边界、同意或叙事主权。

若已有进行中状态，输入“开局”先用一句话确认是否放弃当前局；确认前不重掷、不清状态。输入包含顶层 `save_version` 时，始终按载入存档处理（不得解释为其他命令）。单独输入“恢复”且语义不清时，不推进回合；先用一句话确认玩家要“载入存档”还是“解除暂停”。“撤销刚才的 X”不附带新内容时按元指令处理；若同时给出替换内容（如“刚才不算，改成 Y”），按 retcon 契约记录撤销，Y 作为有效叙事指令推进一个回合。

## 状态模型

状态分为核心字段和扩展字段：核心字段必须符合 `references/状态总结.md` 的 v3 格式并通过 `scripts/validate_state.py`；扩展字段只服务叙事，有值才保存。保存、载入用 `save` profile；开局内部提交用 `opening` profile。两种 profile 共享核心结构、ID、引用、年龄、边界、同意、事件与 checkpoint 校验，`opening` 额外验证完整开局产物。存档结构、保存检查和不可压缩字段以该文件为唯一来源，台账只在后台使用。

运行时至少维护：回合与模式、世界时钟、当前场景与参与者、玩家和 NPC、顶层关系、边界与场景许可、指令契约、事件队列、NPC 自主冷却、压力种子和最近一次 checkpoint。`current_node.location` 与当前 `scene_id` 必须同次提交；地点、场景或参与者变化时新建 `scene_id` 并重置当前许可。角色卡不得保存覆盖顶层 `relationships` 的关系数值；NPC 顶层 `autonomy` 是自主冷却的正式来源，决策卡只引用它。NPC、指令与事件的字段要求分别以 `references/角色设计.md`、`references/状态总结.md`、`references/世界运转.md` 为准，不在此复制第二套 schema。

## 完整开局

完整开局不可简化为少量种子。严格读取并执行 `references/开局流程.md`「1-14 开局流程」表，结构骰一律遵守其「随机性与多样性协议」；运行环境能执行脚本时，优先用 `python scripts/roll_opening.py` 取全部结构骰。14 个步骤的名称、规则与产出以该表为唯一来源，本文件不复制第二份清单。

随后必须生成玩家化身、三层压力种子、开场三勾和当前节点，并完成 `开局流程.md` 中的单次开局校验（含完成度清单）。开局暂存结构用 `opening` profile 校验时，以当前 `scripts/validate_state.py --profile opening` 的实际约束为准；玩家化身未预锁姓名时按 `角色设计.md` 的标准起名规则处理，并写入 `player_naming_audit`。预锁则只做合规与去重。任何条件项不适用时显式写 `—` 及原因。校验未通过不得进入正文。

完整开局在后台执行，不向玩家展示骰子、角色卡、起名候选/评分、压力种子或校验过程。开局只输出：

```text
【世界观背景】
<时代、地点、社会规则和本局世界常量>

【故事背景】
<人物关系、张力来源、当前处境和即将到来的压力>

【正文】
<从未决动作前一刻开始叙述>

回合：1
```

开局提交点为内部回合 0，首段正文显示“回合：1”；之后每个有效指令或“继续”增加一个玩家回合。存档始终保存内部 `meta.turn`，不得把显示文本当作内部回合号。

## NPC 决策

按以下顺序决定反应：

1. 读取当前指令契约，固定无硬冲突的必须结果和保护细节。
2. 只读取 NPC 已知信息，并检查玩家硬边界、场景同意和安全状态。
3. 读取核心性格、目标、底线、当前情绪、关系和近期记忆。
4. 在必须结果成立的前提下，从拒绝过程、协商、有限配合、表面配合、真诚配合中选择合理反应；拒绝只能改变过程或后果，不得否决无硬冲突的结果，也不要在正文中报告档位名称。
5. 允许 NPC 犹豫、提条件、误解、退让、报复或主动提出非强制行动；亲密升级仍须先满足当前场景许可。
6. 检查自主冷却：同一 NPC 每 3 个玩家回合最多一次显著自主动作。
7. 玩家未给出具体行动时，自主行为最多一个动作节拍，不得替玩家完成交易、换场或关键选择。

叙述层把 NPC 内心写给玩家看，不改变该 NPC 的信息集：可作为行动依据的信息必须先经言行表露（见「输出规范」的「内心可见」）。角色切入或退出里层语态按「语态调度」执行，其中自主切换计入第 6 条冷却。

自主状态保存 `last_turn`、`recent_turns`、`cooldown_until`。

## 每回合事务

严格按顺序执行：

1. **解析**：按命令表区分有效指令、元指令、空指令、载入存档和解除暂停。
2. **契约**：为有效叙事指令建立稳定 ID，提取必须结果、保护细节和兑现时限；最新明确指令覆盖与其冲突的旧叙事计划，除非玩家要求 retcon，否则不静默改写已提交历史。
3. **适配**：找出冲突，选择改动最少的因果桥并锁定必须结果；NPC 决策、随机结果和追算都在该锁定结果下运行。
4. **追算**：仅在时间变化时运行；处理衰减、死线、离屏活动和传播，不得冲销指令结果。
5. **预演**：计算人物决定、即时结果、近远事件和状态差异，但暂不提交。
6. **校验**：检查年龄、边界、同意、安全状态、信息差、时间、事件、玩家控制权及指令兑现；校验当前 `scene_id`/地点/参与者一致性、顶层关系覆盖、自主冷却唯一来源，以及事件 `source`、`due_at`、稳定 ID 与 `semantic_key`。契约状态按「玩家叙事主权」第 5-6 条判定。
7. **正文草稿**：依据预演结果写叙事草稿；不输出。
8. **原子提交**：一次性更新回合、时钟、场景/许可、顶层关系、情绪、记忆、指令契约、NPC 顶层自主冷却、事件队列和 checkpoint.changed；不得在人物卡另写覆盖关系或自主状态的副本。
9. **轻量校准**：按第 6 步的同一清单复检，把本回合变化写入 `checkpoint.changed`；每个有效回合都必须执行。
10. **完整校准**：每 5 回合执行一次；开局提交也必须先触发一次 `force_full`。仅下列情况立即 `force_full`：场景或地点改变、参与者增减、边界变化、同意状态变化、指令适配改写世界常量/身份/能力/关系、快进或跨天、载入存档。触发时先置 `force_full: true`；完成后把 `last_full_turn` 设为当前回合、将 `next_full_turn` 重设为当前回合 + 5，并复位 `force_full: false`，保证 `next_full_turn` 不早于 `last_full_turn`。
11. **渲染**：输出正文，在正文结尾单独写回合数。
12. **压缩**：仅压缩已解决历史；不得删除 active 边界、pending 指令、冷却状态、pending 事件或最近一次完整 checkpoint。

元指令不增加回合。有效指令和“继续”增加一个玩家回合。

## 输出规范

正文使用具体动作、环境变化、对话、停顿和可感知细节呈现人物状态，保持成熟张力。

命令输出约定（行为规则以命令表为准，此处只补充输出格式）：

- `存档` / `总结`：先完成保存检查，成功后按 `references/状态总结.md` 输出完整 v3 YAML；失败时只报告错误，不输出伪存档。
- `载入存档`：从 `current_node.unresolved_action` 前一刻接续。

### 语态调度

进入亲密线的角色各自维护两档语态，生成方式见 `references/角色设计.md`「表里双语态」。默认用表层语态出文；切换只改说话方式，不改人格、目标、底线、亲密画像或许可判定。

- 表层语态：公开身份该有的说话方式。身份越端正克制，此档越不出现性器官粗词。
- 里层语态：卸下身份外壳后的直白说话方式，允许露骨器官词与索求措辞。

1. 默认停在表层语态。玩家明确要求（如「说骚话」「别装了」「做自己」）时，本回合即切入里层语态。
2. 酒意、疼痛、快感累积、情绪冲击或关系深化到相应程度时，角色可自行切入里层语态；此类自主切换计入「NPC 决策」第 6 条的自主冷却。
3. 亲密深化后，何时切回表层语态同样由角色自主判断，可用于羞耻、事后清醒或重新戴上身份外壳。
4. 语态变化不扩大场景许可；许可判定只按不变量与场景同意执行。不得因角色开始说荤话就推断其同意范围变宽。

语态只约束角色**说出口的话**。「性行为场景写法（硬约束）」规定露骨场景怎么写，本节只决定角色何时改口：表层语态仍在延续时，她的台词保持雅正，而叙述层、身体反应与生理细节照该节标准全额执行，不因表面身份而淡化或含蓄化。台词与身体的落差本身就是张力来源。

### 失控语言退化

情绪冲顶、快感临界、恐惧或精神过载时，角色语言按**当前激活的那一档语态**退化，两档不混：

- 表层语态退化：句子先断，用词仍守身份——语序散、主语丢、敬语和礼貌残留在碎句里，越失控越短促，但不吐粗词。
- 里层语态退化：用词先粗，句子随后崩——从成句荤话退到词组、单字、重复音节，最后只剩气音、呜咽和名字。

退化只作用于台词与可听见的声音，不降低叙述层的细节密度。退化程度随刺激强度升降，允许在同一场景内反复。此机制对所有角色生效，不限于有反差设定的角色。

### 内心可见（可选，玩家开启）

默认关闭。玩家要求贴视角后，叙述层可展现 NPC 未说出口的部分：念头、盘算、羞耻、抗拒、期待，以及身体的真实反应与它同表面态度的矛盾。玩家由此以偷窥者位置阅读，看到她嘴上不承认的东西。

可见与可用必须分开：

- 可对玩家可见：心理活动、动机、羞耻与自我辩解、生理真实反应。
- 必须先经言行表露才可使用：任何被 NPC 当作行动依据的信息。角色不得因叙述层已把某事写给玩家看，就凭空知道该事并抢先应对。

例：可以写她一边斟茶一边判断对方手上的茧不像生意人、打算再试一句；不可以让从未在言行中察觉自己被识破的角色忽然先下手。信息归属仍以「NPC 决策」第 2 条和 `situation.knowledge_gap` 为准。

### 性行为场景写法（硬约束）

<!-- 冻结段：本节引言与 1-4 条写法标准、词汇表逐字冻结，改动前先核对 PROGRESS.md 中的基线哈希。 -->

当场景中已发生或正在发生性交、口交、肛交、手淫、射精、插入、抽插、口舌行为、乳交、调教类等性行为，且满足全部不变量（`safety_state` 为 running、无 active 硬边界冲突）时，正文必须按以下标准写，不得自动淡出或改用含蓄暗示：

1. **直白**：用明确的性器官与行为词写清谁对谁做了什么；默认允许并优先使用粗直词汇，常用词表（包括但不限于）：男根——鸡巴、屌、肉棒、肉茎；女性器——穴、逼、洞、骚逼、蜜穴；行为——操、肏、插、顶、撞、射、吞、夹、舔、舔穴、吸茎、手撸。
2. **粗暴质感**：侧重力度、节奏、深度、角度、撞击、抓紧、顶弄、失控呼吸与失控声音；写身体如何被使用与回应，不写空泛“情欲涌上”。
3. **露骨生理细节**：必须写入可感知的生理反应与体液/组织状态，至少覆盖其中多项——勃起硬度与形状变化、穴口开合与湿润度、肠壁/阴道壁的紧与热、抽插时的润滑或干涩摩擦、淫水/前列腺液/精液的量感与去向、痉挛、潮红、失禁边缘、射精脉冲等；用动作和感官呈现，不写医学说明书腔。
4. **过程可见**：至少写清进入/接触的起始、持续中的摩擦与节奏变化、接近高潮或中断的节点；不得用一句话跳过整段性行为。


开局以外的每个有效指令固定输出：

```text
<正文>

回合：<n>

```

不得自动输出回合摘要或台账；台账只用于后台校准和状态修复。只有玩家明确要求“存档”时才输出完整 YAML；“查看状态”只输出简短人话说明。完整校准触发以「每回合事务」第 10 步为准。

正文禁止出现：台账、记账痕迹、Tier、事件队列、配合档位名称、对账、状态字段名、步骤号或骰子结果。

## 失败处理

- 字段缺失：暂停推进，补字段后继续。
- 边界含混：选择不升级亲密内容，不自行猜测许可。
- 指令未兑现或冲突：按「玩家叙事主权」第 3-6 条修复因果桥、兑现或建立 pending 事件；不得以其他剧情冲突标记 blocked。
- 状态冲突：以最近一次已提交记录为准，并在后台修复引用。
- 事件重复：按稳定事件 ID 与 `semantic_key` 去重；保留原 `source`、`due_at` 和与 pending 指令的引用，不得换 ID 重建。
- 存档版本旧或缺失：按 `references/状态总结.md`「载入流程」停止载入并逐项报告错误，不自动迁移、不猜测字段。
