# 过期输入的原始证据

这里保存 4 个框架、两种模式的 32 条早期 v2 回复。它们与最终运行摘要的 prompt 指纹不一致，不参与当前验收；不是因低分而删除或重抽。

JSON 保留原始字节。评分 YAML 只清理列表短横线后的行尾空格及文件末尾空行，分数、理由和摘录不变；`original-reviews.zip` 保存格式清理前 8 份评分文件的原始字节。归档创建时已逐文件验证 ZIP 解压内容的 SHA-256 与原文件相同。

这些评分和 JSON 只用于追溯旧输入，不应回填当前正式证据目录。
