# 素材架构

本文件是职责与素材分层的总导航。运行时按生成聚合与核心 YAML 池加载，维护时按单框架源编辑；新增素材先确定所属层，再检查跨层兼容，不扩大普通回合上下文。

机器可读索引见 `references/material_architecture.yaml`，它只引用现有数据路径，不复制素材内容。全量盘点使用 `python scripts/material_inventory.py`；输出包含每个叶级条目的文件、路径、所属层、模式、未归类文件和层级统计，不会被游玩流程加载。八层是逻辑归属，不移动现有文件、不新增抽取阶段，也不改变存档格式。本次只整理非露骨叙事素材的维护入口，其他素材和生成规则保持原状。

维护登记见 `references/material_registry.yaml`，由 `python scripts/sync_governance.py --write` 汇入当前来源、人工 review 与精确清理记录后紧凑写入。它保留决定和 history；来源哈希变化使旧审查失效。仅完成登记不授予运行资格。

## 职责导航

| 入口 | 唯一职责 |
|---|---|
| `SKILL.md`、`commands.yaml` | 运行顺序与命令行为；仅新局用世界观、人物、正文三个标题，前两项各 1-2 句 |
| `references/状态总结.md` | v3 schema 与原载入入口；会话绑定不进入 schema |
| `references/运行状态速览.md` | session/state_token、pending 上限、终态回执与 live_slice --event 定向读取 |
| `authoring/framework_index.yaml`、`authoring/frameworks/` | 稳定框架 ID、顺序和单框架编辑源 |
| `scripts/data/world_frameworks.yaml` | build_frameworks 生成的运行聚合，禁止手改 |
| `maintenance/data_manifest.yaml` | 数据文件职责与依赖，QA 据此计算影响范围 |
| `references/material_architecture.yaml` | 八层逻辑选择器，不替代文件依赖清单 |
| `maintenance/core_review_decisions.yaml` | 核心池人工处置输入，声明读过的内容与适用边界 |
| `maintenance/framework_reviews/*.yaml` | 单框架当前来源哈希、语义检查、理由及配套审查 |
| `maintenance/duplicate_reviews.yaml`、`scripts/check_duplicates.py` | 重复候选的人工保留理由与核验；区分语义差异、分类索引引用，不自动删重 |
| `references/material_registry.yaml` | 紧凑维护登记与可追溯 history，不是运行时内容副本 |
| `maintenance/baseline.yaml` | 冻结整节 SHA256、用户框架选择与实玩要求 |
| `maintenance/playtests/` | 真实模型响应及独立评分；playtest_report 只核验证据 |
| `scripts/qa.py`、`references/加内容.md` | 验证入口与 L0-L3 工作流；run_playtest 须另行显式调用 |

## 1. 世界框架层

编辑源：`authoring/frameworks/<ID>.yaml`；生成文件：`scripts/data/world_frameworks.yaml`。当前保留 40 个框架，数量不代表全部审查或实玩通过。

负责定义一个可以独立成立的世界包：时代、规则、美学、社会秩序、地点、人物搭配、日常活动、地方细节，以及压力模式的来源、触发、人物影响和出口。

世界框架必须同时支持：

- 日常模式：有具体活动和自然见面理由，不自动生成外部危机链。
- 压力模式：压力来自框架规则、资源、制度、环境或关系，而不是孤立套用通用压力词。

## 2. 场景空间层

文件：`scripts/data/pools.yaml`、`locations.yaml`、`location_profiles.yaml`

天气、季节与技术边界目前没有统一的独立素材池，可由框架规则和地点细节承载；本次不将其标为已实现的抽取轴。

地点条目负责名称和基本语义；地点画像负责隐私、可见度、见证者、出口、可执行动作和压力修饰。时代专属地点必须同步 `meta.location_eras`。

## 3. 人物层

文件：`identities.yaml`、`identity_profiles.yaml`、`character_meta.yaml`、`character_pools.yaml`、`names.yaml`

人物素材包括玩家身份、NPC身份、社会位置、年龄、称谓、外观气质、性格、资源、目标、边界、说话方式和压力反应。新增人物必须能解释其为何出现在当前地点，以及其能力和限制如何影响活动。

命名不局限现代姓名，以时代、文化和生活背景为准；汉化书写不改人物文化归属。

## 4. 关系层

文件：`character_meta.yaml`、`pools.yaml`、`world_frameworks.yaml`

关系素材包括初始关系、相识理由、公开关系、私人期待、信任、信息差、持续往来理由和退出条件。新增关系应能转化为一次自然见面或共同活动。现有来源是 `character_meta.yaml -> 社会位置关系` 与框架 `pairs[].relationship_reason`；信任等动态状态仍归存档，不在素材索引中复制。私人期待、持续往来理由和退出条件目前没有统一的独立素材池，可在关系理由及活动后续中表达。

## 5. 活动与互动层

文件：`action_categories.yaml`、`action_metadata.yaml`、`pools.yaml`、`templates.yaml`

活动是日常和压力模式共用的可玩入口。经营、探索、创作、社交和休闲是选材方向，并非已新增的运行枚举；实际 `category` 继续使用 `action_categories.yaml` 的既有分类，框架活动通过 `places` 和 `pairs` 关联地点与人物。具体动作必须符合人物能力和地点 affordances；涉及压力时再连接压力情境，不把每个活动强制升级为危机。

## 6. 开局情境层

文件：`pools.yaml`、`templates.yaml`、`world_frameworks.yaml`

- 日常：当前活动、相遇契机、新鲜变化、可接手动作、自然后续。
- 压力：压力来源、触发、受影响对象、当下目标、选择、出口和后果。

框架现有字段映射：`source` 是来源；`beats.trigger` 是触发；`beats.objective/choice` 是目标与选择；`beats.immediate/near` 是即时影响及后续；`far_trigger/far_consequence` 是远期压力事件。出口目前写入可选行动与后续文字，没有独立 `pressure_exits` 字段；不要仅新增一个未被读取的字段就声称运行时支持。时限按情境需要填写，不要求所有压力都有倒计时。

压力来源、处境和张力引擎保持分层：来源是外部由头，处境是人物正在承受的问题，张力引擎是整晚的动态戏路。

## 7. 后续变化层

文件：`twists.yaml`、`twist_profiles.yaml`、`references/世界运转.md`

压力转折库负责新信息、第三方介入、资源变化等；日常框架的自然后续来自 `activities.*.beats.near`。索引中的 `modes` 是用途说明，不是新的运行时开关；不会因本次分类让日常开局自动调用压力转折库。人物离屏行动和阶段收束继续由世界运转协议处理。转折必须改变至少一项可观察的事实、选择或后续条件。

## 8. 表达层

文件：`templates.yaml`、`SKILL.md` 及相关 references

负责把已确定的世界、人物、活动和情境写成玩家可见正文。表达模板不能反过来决定世界事实；事实先由素材和状态确定。

## 跨层兼容检查

新增或修改素材后，至少检查：

1. 世界框架与时代、地点、技术、美学、社会规则一致。
2. 地点与人物身份、玩家社会位置、活动和场所条件一致。
3. 人物外观、称谓、身份行为与主题和时代一致。
4. 关系理由能解释双方如何认识、为何继续见面。
5. 日常活动有可执行动作；压力情境有明确出口。
6. 新条目不会与同层条目逐字重复，也不会把压力来源、处境和张力引擎混为一谈。

## 新增素材填写方式

1. 核心素材在原数据文件编辑，框架在 authoring 单文件编辑；按 `references/加内容.md` 补齐现有字段，不手改运行聚合或复制框架专属项到通用池。
2. 新增数据文件须登记 `maintenance/data_manifest.yaml` 的职责与依赖；新增字段路径才补 `material_architecture.yaml` 的对应层。`path: []` 表示根节点，`*` 表示所有映射值或列表成员。
3. 两种模式共享项不写来源级 `modes`；仅日常或压力使用的项明确写 `[daily]` 或 `[pressure]`。同一来源可以被不同层引用，不复制正文。
4. 运行下列检查；若实际修改抽取或填料行为，再运行相应开局测试。引用校验不判断文案是否有趣或语义是否合理，这些仍需审读。

验收命令：

```powershell
python -X utf8 scripts/build_frameworks.py --write
python -X utf8 scripts/sync_governance.py --write
python -X utf8 scripts/qa.py --changed
```

`check_content.py` 已调用框架检查；`check_frameworks.py` 不是独立命令行入口。

## 验证与发布

日常顺序是编辑 authoring → build_frameworks --write → sync_governance --write → qa。构建后才更新运行聚合，同步后才写入维护 registry；人工 review 仍需先阅读实际内容和消费者。默认 auto 仅使用来源哈希一致且审查有效的框架，无隐式 legacy 回退；独立旧池必须显式 `--framework legacy`。

`qa.py --changed` 按改动与依赖选范围；只有单框架源或 review 及其生成聚合、registry、指纹一起变化时，抽样仍保持框架范围，其他共享改动可扩大范围。`--framework <名称>` 与 `--material <框架ID>` 定向到框架，其他素材 ID 按文件依赖选择。`--full` 做全量结构与回归，分布分析采用默认 auto/pressure 的 1000 次抽样。`--full --release` 另核验当前语义与实玩证据，`--full --update-fingerprint` 在检查通过后更新指纹，`--plan` 只读计划。详细参数组合与 L0-L3 最低要求见 `references/加内容.md`，不在这里复制脚本清单。

盘点字段数、名称引用数和内存开局数是不同分母。**结构通过不等于语义通过，不等于 320 条实玩响应已完成。** 发布要求 DeepSeek Harness 上 40 框架 × 两模式 × 新局及 3 次续写，并有逐回合独立评分；`playtest_report.py` 检查已有证据。核心 `BRIDGE_REQUIRED` 可以登记，但尚未闭合时会阻止精品化发布。`run_playtest.py` 须显式调用并消耗模型额度，默认 QA 不会自动调用它，也不会补写评分。当前探针与完整宿主端到端验收的范围差异见 `maintenance/implementation_report.md`。

可证实不用的对象按 `verified_direct` 直接清理，但必须登记精确来源、旧哈希、消费者与引用搜索、原因和实际回归证据；冻结对象、共享依赖与旧存档不在清理范围。`references/governance_decisions.yaml` 的 10 份旧 review 因失效或框架被排除已删除，原文可查 git 和登记 history，现用 `maintenance/framework_reviews/*.yaml`。

报告状态：CLASSIFIED 仅表示字段有架构归属；REFERENCED 仅表示对应旧池类型有框架同名引用；UNMAPPED 表示缺少归属或配套证据；BRIDGE 表示现有约束冲突需要人工判断，不是自动放行。NOT_REVIEWED 明确表示未获得逐项语义审查结论。报告保留这些差异，不将全部归类称为全部闭合。
